export interface LogListType {
  author: string;
  cTime: string;
  classification: string;
  edittype: string;
  isShow: string;
  isStick: string;
  log_id: string;
  mTime: string;
  title: string;
};

export const a = 1;  // 这个用来绕过 export interface 的报错